<UserControl x:Class="His_Pos.H5_ATTEND.ClockIn.ClockInView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:His_Pos.H5_ATTEND.ClockIn"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="839" d:DesignWidth="1365">
    <Viewbox Stretch="Fill">
        <Grid Width="1365" Height="839">
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="50"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="400"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Row="0" Grid.Column="0" Content="今日上班人員" FontSize="25" FontFamily="Segoe UI SemiBold" VerticalAlignment="Center" Margin="10,0,0,0"/>
            <StackPanel Name="UserIconStack" Orientation="Horizontal" Grid.Row="1" Grid.Column="0">
                
            </StackPanel>
            <Border Grid.Row="2" Grid.Column="0" Height="480" Width="450" Margin="0,0,0,50" BorderThickness="1,1,3,3" BorderBrush="{StaticResource DarkShadow}">
                <Grid Height="480" Width="450" Background="{StaticResource GridBackGround}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="4*"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="70"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="70"/>
                        <RowDefinition Height="5*"/>
                    </Grid.RowDefinitions>
                    <Label Grid.Row="1" Grid.Column="0" Content="上下班打卡" FontSize="35" FontFamily="Segoe UI SemiBold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="0" HorizontalAlignment="Center">
                        <TextBlock Grid.Row="2" Grid.Column="0" Name="SystemTime"  FontSize="15" TextWrapping="Wrap" Margin="10,5,10,10"  VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Name="InOutStack" Orientation="Horizontal" Grid.Row="3" Grid.Column="0" HorizontalAlignment="Center">
                        <RadioButton Content="上班" Tag="I" FontSize="30" FontFamily="Segoe UI Semibold" VerticalAlignment="Center" VerticalContentAlignment="Center" IsChecked="True"/>
                        <RadioButton Content="下班" Tag="O" Margin="100,0,0,0" FontSize="30" FontFamily="Segoe UI Semibold" VerticalAlignment="Center" VerticalContentAlignment="Center"/>
                    </StackPanel>
                    <TextBox Name="UserId" Grid.Row="4" Grid.Column="0" materialDesign:HintAssist.Hint="員工編號" Width="200" FontSize="25" HorizontalContentAlignment="Center" FontFamily="Segoe UI SemiBold" HorizontalAlignment="Center" KeyDown="UserId_KeyDown"/>
                    <PasswordBox Name="UserPassword" Grid.Row="5" Grid.Column="0" materialDesign:HintAssist.Hint="員工密碼" Width="200" FontSize="25" HorizontalContentAlignment="Center" FontFamily="Segoe UI SemiBold" HorizontalAlignment="Center" KeyDown="UserPassword_KeyDown"/>
                    <Button Grid.Row="6" Grid.Column="0" Width="150" Content="打卡" FontFamily="Segoe UI Semibold" FontSize="25" Background="DeepSkyBlue" BorderThickness="0" Height="40" Click="ClockIn_Click"/>
                </Grid>
            </Border>
            <DataGrid Grid.Row="0" Grid.Column="1" Grid.RowSpan="3" Background="{StaticResource GridBackGround}" CanUserAddRows="False" ItemsSource="{Binding EmpClockIns,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                      CellStyle="{StaticResource DataGridCellStyle}" ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}" HorizontalScrollBarVisibility="Disabled"
                      AutoGenerateColumns="False" SelectionChanged="DataGrid_SelectionChanged">
                <DataGrid.Resources>
                    <Style TargetType="{x:Type ScrollBar}">
                        <Setter Property="SnapsToDevicePixels" Value="True"/>
                        <Setter Property="OverridesDefaultStyle" Value="true"/>
                        <Setter Property="Width" Value="100"/>
                        <Style.Triggers>
                            <Trigger Property="Orientation" Value="Horizontal">
                                <Setter Property="Width" Value="Auto" />
                                <Setter Property="Height" Value="15" />
                                <Setter Property="Template" Value="{StaticResource HorizontalScrollBar}" />
                            </Trigger>
                            <Trigger Property="Orientation" Value="Vertical">
                                <Setter Property="Width" Value="15"/>
                                <Setter Property="Height" Value="Auto" />
                                <Setter Property="Template" Value="{StaticResource VerticalScrollBar}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.RowStyle>
                    <Style TargetType="{x:Type DataGridRow}">
                        <Style.Triggers>
                            <Trigger Property="DataGridRow.IsSelected" Value="True">
                                <Setter Property="Background" Value="{StaticResource GridSelected}"/>
                            </Trigger>
                        </Style.Triggers>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Height" Value="40"/>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn MinWidth="120" MaxWidth="120" Header="員工姓名" FontSize="16" FontFamily="Segoe UI Semi" IsReadOnly="True" Binding="{Binding empName,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="{x:Type TextBlock}">
                                <Setter Property="HorizontalAlignment"  Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn MinWidth="80" MaxWidth="100" Header="上下班" FontSize="16" FontFamily="Segoe UI Semi" IsReadOnly="True" Binding="{Binding clocckType,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="{x:Type TextBlock}">
                                <Setter Property="HorizontalAlignment"  Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn MinWidth="80" MaxWidth="100" Header="日期" FontSize="16" FontFamily="Segoe UI Semi" IsReadOnly="True" Binding="{Binding clocckDate,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="{x:Type TextBlock}">
                                <Setter Property="HorizontalAlignment"  Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn MinWidth="100" MaxWidth="220" Header="打卡時間" FontSize="16" FontFamily="Segoe UI Semi" IsReadOnly="True" Binding="{Binding clockTime,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="{x:Type TextBlock}">
                                <Setter Property="HorizontalAlignment"  Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Viewbox>
</UserControl>
