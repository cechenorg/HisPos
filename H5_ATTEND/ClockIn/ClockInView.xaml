<UserControl x:Class="His_Pos.H5_ATTEND.ClockIn.ClockInView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:His_Pos.H5_ATTEND.ClockIn"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="839" d:DesignWidth="1365">
    <Viewbox Stretch="Fill">
        <Grid Width="1365" Height="839">
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="50"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="400"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Row="0" Grid.Column="0" Content="今日上班人員" FontSize="25" FontFamily="Segoe UI SemiBold" VerticalAlignment="Center" Margin="10,0,0,0"/>
            <StackPanel Name="UserIconStack" Orientation="Horizontal" Grid.Row="1" Grid.Column="0">
                
            </StackPanel>
            <Border Grid.Row="2" Grid.Column="0" Height="480" Width="450" Margin="0,0,0,50" BorderThickness="1,1,3,3" BorderBrush="{StaticResource DarkShadow}">
                <Grid Height="480" Width="450" Background="{StaticResource GridBackGround}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="4*"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="70"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="70"/>
                        <RowDefinition Height="5*"/>
                    </Grid.RowDefinitions>
                    <Label Grid.Row="1" Grid.Column="0" Content="上下班打卡" FontSize="35" FontFamily="Segoe UI SemiBold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="0" HorizontalAlignment="Center">
                        <TextBlock Grid.Row="2" Grid.Column="0" Name="SystemTime"  FontSize="15" TextWrapping="Wrap" Margin="10,5,10,10"  VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Name="InOutStack" Orientation="Horizontal" Grid.Row="3" Grid.Column="0" HorizontalAlignment="Center">
                        <RadioButton Content="上班" Tag="I" FontSize="30" FontFamily="Segoe UI Semibold" VerticalAlignment="Center" VerticalContentAlignment="Center"/>
                        <RadioButton Content="下班" Tag="O" Margin="30,0,0,0" FontSize="30" FontFamily="Segoe UI Semibold" VerticalAlignment="Center" VerticalContentAlignment="Center"/>
                        <RadioButton Content="自動辨識" Tag="A" Margin="30,0,0,0" FontSize="30" FontFamily="Segoe UI Semibold" VerticalAlignment="Center" VerticalContentAlignment="Center" IsChecked="True"/>
                    </StackPanel>
                    <TextBox Name="UserId" Grid.Row="4" Grid.Column="0" materialDesign:HintAssist.Hint="員工編號" Width="200" FontSize="25" HorizontalContentAlignment="Center" FontFamily="Segoe UI SemiBold" HorizontalAlignment="Center" KeyDown="UserId_KeyDown"/>
                    <PasswordBox Name="UserPassword" Grid.Row="5" Grid.Column="0" materialDesign:HintAssist.Hint="員工密碼" Width="200" FontSize="25" HorizontalContentAlignment="Center" FontFamily="Segoe UI SemiBold" HorizontalAlignment="Center" KeyDown="UserPassword_KeyDown"/>
                    <Button Grid.Row="6" Grid.Column="0" Width="150" Content="打卡" FontFamily="Segoe UI Semibold" FontSize="25" Background="DeepSkyBlue" BorderThickness="0" Height="40" Click="ClockIn_Click"/>
                </Grid>
            </Border>
            <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="1" Margin="0,0,10,0"  Background="{StaticResource GridBackGround}">
                <Label Content="員工" FontSize="20" FontFamily="Segoe UI SemiBold" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,0,0,0"/>
                <ComboBox Name="comboboxEmployee" ItemsSource="{Binding Employees,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" Width="120" FontSize="16" FontFamily="Segoe UI SemiBold" VerticalAlignment="Center" Margin="10,10,0,10" SelectionChanged="ComboBox_SelectionChanged"/>
                <Label Content="日期" FontSize="20" FontFamily="Segoe UI SemiBold" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,0,0,0"/>
                <DatePicker  Width="120" Height="30" Name="start" SelectedDateFormat="Short"  SelectedDateChanged="start_SelectedDateChanged" Margin="10,0,0,0"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="1" Margin="0,0,10,0"  Background="{StaticResource GridBackGround}">
                <RadioButton Content="上班" Tag="0" Margin="35,0,5,0" FontSize="20" FontFamily="Segoe UI Semibold" VerticalAlignment="Center" VerticalContentAlignment="Center" Checked="RadioButton_Checked"/>
                <Image Source="/Images/PosDot.png" Width="15"/>
                <RadioButton Content="下班" Tag="1" Margin="45,0,5,0" FontSize="20" FontFamily="Segoe UI Semibold" VerticalAlignment="Center" VerticalContentAlignment="Center" Checked="RadioButton_Checked"/>
                <Image Source="/Images/DarkerHisDot.png" Width="15"/>
                <RadioButton Content="全部" Tag="2" Margin="45,0,0,0" FontSize="20" FontFamily="Segoe UI Semibold" VerticalAlignment="Center" VerticalContentAlignment="Center" Checked="RadioButton_Checked" IsChecked="True"/>
            </StackPanel>
            <DataGrid Name="DataGridClockIn" Grid.Row="2" Grid.Column="1" Grid.RowSpan="1" Margin="0,0,10,10" Background="{StaticResource GridDarkerBackGround}" CanUserAddRows="False" ItemsSource="{Binding EmpClockIns,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                      CellStyle="{StaticResource DataGridCellStyle}" ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}" HorizontalScrollBarVisibility="Disabled"
                      AutoGenerateColumns="False" SelectionChanged="DataGrid_SelectionChanged">
                <DataGrid.Resources>
                    <Style TargetType="{x:Type ScrollBar}">
                        <Setter Property="SnapsToDevicePixels" Value="True"/>
                        <Setter Property="OverridesDefaultStyle" Value="true"/>
                        <Setter Property="Width" Value="100"/>
                        <Style.Triggers>
                            <Trigger Property="Orientation" Value="Horizontal">
                                <Setter Property="Width" Value="Auto" />
                                <Setter Property="Height" Value="15" />
                                <Setter Property="Template" Value="{StaticResource HorizontalScrollBar}" />
                            </Trigger>
                            <Trigger Property="Orientation" Value="Vertical">
                                <Setter Property="Width" Value="15"/>
                                <Setter Property="Height" Value="Auto" />
                                <Setter Property="Template" Value="{StaticResource VerticalScrollBar}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.RowStyle>
                    <Style TargetType="{x:Type DataGridRow}">
                        <Style.Triggers>
                            <Trigger Property="DataGridRow.IsSelected" Value="True">
                                <Setter Property="Background" Value="{StaticResource GridSelected}"/>
                            </Trigger>
                        </Style.Triggers>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Height" Value="40"/>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTemplateColumn Width="40">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Image Source="{Binding typeIcon, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="20" Margin="10,0,5,0" Grid.Column="0" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Width="70" Header="上下班" FontSize="16" FontFamily="Segoe UI Semi" IsReadOnly="True" Binding="{Binding clocckType,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="{x:Type TextBlock}">
                                <Setter Property="HorizontalAlignment"  Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Width="120" Header="員工姓名" FontSize="16" FontFamily="Segoe UI Semi" IsReadOnly="True" Binding="{Binding empName,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="{x:Type TextBlock}">
                                <Setter Property="HorizontalAlignment"  Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Width="150" Header="日期" FontSize="16" FontFamily="Segoe UI Semi" IsReadOnly="True" Binding="{Binding clocckDate,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="{x:Type TextBlock}">
                                <Setter Property="HorizontalAlignment"  Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Viewbox>
</UserControl>
