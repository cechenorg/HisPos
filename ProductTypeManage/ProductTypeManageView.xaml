<UserControl x:Class="His_Pos.ProductTypeManage.ProductTypeManageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:His_Pos.ProductTypeManage"
             mc:Ignorable="d" 
             d:DesignHeight="839" d:DesignWidth="1365">
    <Viewbox Stretch="Fill">
        <Grid Width="1365" Height="839">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition/>
                <RowDefinition Height="50"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Label Content="OTC" Grid.Column="0" Grid.Row="0" Foreground="Black" FontFamily="Segoe UI Semibold" FontSize="30" VerticalAlignment="Center" Margin="10,0,0,0"/>
            <DataGrid Grid.Column="0" Grid.Row="1" Margin="10,10,0,10" HeadersVisibility="None" Background="{StaticResource GridBackGround}"
                      CellStyle="{StaticResource DataGridCellStyle}" ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn/>
                </DataGrid.Columns>
            </DataGrid>
            <DataGrid Grid.Column="1" Grid.Row="1" Margin="0,10,10,10" HeadersVisibility="None" Background="{StaticResource GridDarkerBackGround}"
                      CellStyle="{StaticResource DataGridCellStyle}" ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn/>
                </DataGrid.Columns>
            </DataGrid>
            <Grid Grid.Column="2" Grid.Row="1" Margin="0,10,10,10" Background="{StaticResource GridDarkestBackGround}">
                
            </Grid>
            <Label Content="MEDICINE" Grid.Column="0" Grid.Row="2" Foreground="Black" FontFamily="Segoe UI Semibold" FontSize="30" VerticalAlignment="Center" Margin="10,0,0,0"/>
            <DataGrid Grid.Column="0" Grid.Row="3" Margin="10,10,0,10" HeadersVisibility="None" Background="{StaticResource GridBackGround}"
                      CellStyle="{StaticResource DataGridCellStyle}" ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn/>
                </DataGrid.Columns>
            </DataGrid>
            <DataGrid Grid.Column="1" Grid.Row="3" Margin="0,10,10,10" HeadersVisibility="None" Background="{StaticResource GridDarkerBackGround}"
                      CellStyle="{StaticResource DataGridCellStyle}" ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn/>
                </DataGrid.Columns>
            </DataGrid>
            <Grid Grid.Column="2" Grid.Row="3" Margin="0,10,10,10" Background="{StaticResource GridDarkestBackGround}">

            </Grid>
        </Grid>
    </Viewbox>
</UserControl>
