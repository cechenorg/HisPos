<Window x:Class="His_Pos.H1_DECLARE.PrescriptionDec2.DiseaseCodeSelectDialog"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:His_Pos.H1_DECLARE.PrescriptionDec2"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="疾病代碼" MinHeight="600" Height="600" MinWidth="800" Width="800" WindowStartupLocation="CenterScreen" >
    <Window.Resources>
        <Style TargetType="{x:Type TextBlock}" x:Key="TextInMiddleCellStyle">
            <Setter Property="HorizontalAlignment"  Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    <Viewbox Stretch="Fill">
        <Grid Name="ItemGrid"  Width="800" Height="600" HorizontalAlignment="Left">
            <Grid.RowDefinitions>
                <RowDefinition Height="550"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>
            <DataGrid Name="SearchResult" Grid.Row="0" ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}" CellStyle="{StaticResource DataGridCellStyle}"  HorizontalAlignment="Left"
                      AutoGenerateColumns="False" FontFamily="Segoe UI Semi" HorizontalScrollBarVisibility="Hidden" Margin="10" Background="{StaticResource GridBackGround}"
                  ItemsSource="{Binding DiseaseCollection,UpdateSourceTrigger=PropertyChanged}">
                <DataGrid.Resources>
                    <Style TargetType="{x:Type ScrollBar}">
                        <Setter Property="SnapsToDevicePixels" Value="True"/>
                        <Setter Property="OverridesDefaultStyle" Value="true"/>
                        <Setter Property="Width" Value="100"/>
                        <Style.Triggers>
                            <Trigger Property="Orientation" Value="Horizontal">
                                <Setter Property="Width" Value="Auto"/>
                                <Setter Property="Height" Value="15"/>
                                <Setter Property="Template" Value="{StaticResource HorizontalScrollBar}"/>
                            </Trigger>
                            <Trigger Property="Orientation" Value="Vertical">
                                <Setter Property="Width" Value="15"/>
                                <Setter Property="Height" Value="Auto"/>
                                <Setter Property="Template" Value="{StaticResource VerticalScrollBar}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.RowStyle>
                    <Style TargetType="{x:Type DataGridRow}">
                        <Style.Triggers>
                            <Trigger Property="DataGridRow.IsSelected" Value="True">
                                <Setter Property="Background" Value="{StaticResource GridSelected}"/>
                            </Trigger>
                        </Style.Triggers>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Setter Property="FontSize" Value="18"/>
                        <Setter Property="Height" Value="40"/>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ICD-10" MinWidth="100" ElementStyle="{StaticResource TextInMiddleCellStyle}" Binding="{Binding ICD10.Id,UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" FontFamily="Segoe UI Semibold"/>
                    <DataGridTextColumn Header="名稱" MinWidth="260" Binding="{Binding ICD10.Name,UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" FontFamily="Segoe UI Semibold"/>
                    <DataGridTextColumn Header="CM/PCS" MinWidth="80" ElementStyle="{StaticResource TextInMiddleCellStyle}" Binding="{Binding ICD10_Type,UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" FontFamily="Segoe UI Semibold"/>
                    <DataGridTextColumn Header="ICD-9" MinWidth="70" ElementStyle="{StaticResource TextInMiddleCellStyle}" Binding="{Binding ICD9.Id,UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" FontFamily="Segoe UI Semibold"/>
                    <DataGridTextColumn Header="名稱" MinWidth="260" Binding="{Binding ICD9.Name,UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" FontFamily="Segoe UI Semibold"/>
                </DataGrid.Columns>
            </DataGrid>
            <Button Content="確認" Grid.Row="1"  Width="200" Height="30" Background="RoyalBlue" BorderBrush="Transparent" FontSize="18" FontFamily="Segoe UI Semibold" Click="ConfirmClick"/>
        </Grid>
    </Viewbox>
</Window>
