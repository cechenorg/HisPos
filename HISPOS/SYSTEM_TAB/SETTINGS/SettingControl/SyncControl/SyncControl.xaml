<UserControl 
    x:Class="His_Pos.SYSTEM_TAB.SETTINGS.SettingControl.SyncControl.SyncControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:command="http://www.galasoft.ch/mvvmlight"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    xmlns:local="clr-namespace:His_Pos.SYSTEM_TAB.SETTINGS.SettingControl.SyncControl"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
    d:DesignHeight="600"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.DataContext>
        <local:SyncControlViewModel/>
    </UserControl.DataContext>
    <xctk:BusyIndicator BusyContent="{Binding BusyContent, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" IsBusy="{Binding IsBusy, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, RelativeSource={RelativeSource AncestorType={x:Type Window}}}">
        <Grid Background="LightGray">
            <Grid.RowDefinitions>
                <RowDefinition Height="80" />
                <RowDefinition Height="80" />
                <RowDefinition Height="80" />
                <RowDefinition Height="80" />
                <RowDefinition Height="80" />
                <RowDefinition Height="70" />
                <RowDefinition />
                <RowDefinition Height="50" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal">
                <Button Margin="10,0,0,0" Width="auto" Background="Green" BorderThickness="0" Command="{Binding BtnUpdateSingdeMedStockCommand}" Content="更新管藥資料" FontFamily="Segoe UI Semibold" FontSize="16" />
                <Button Margin="10,0,0,0" Width="auto" Background="Green" BorderThickness="0" Command="{Binding BtnUpdateSingdeOTCStockCommand}" Content="更新杏德門市商品資料" FontFamily="Segoe UI Semibold" FontSize="16" />
                <Button Margin="10,0,0,0" Width="auto" Background="Green" BorderThickness="0" Command="{Binding BtnSyncSingdeStockCommand}" Content="更新杏德藥品" FontFamily="Segoe UI Semibold" FontSize="16" />
                <Button Margin="10,0,0,0" Width="auto" Background="Green" BorderThickness="0" Command="{Binding BtnSyncSingdeOTCStockCommand}" Content="更新杏德門市商品資料(價格及庫存量)" FontFamily="Segoe UI Semibold" FontSize="16" />
            </StackPanel>
        </Grid>
    </xctk:BusyIndicator>
</UserControl>
