<UserControl x:Class="His_Pos.SYSTEM_TAB.H2_STOCK_MANAGE.ProductPurchaseReturn.NormalView.NormalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:His_Pos.SYSTEM_TAB.H2_STOCK_MANAGE.ProductPurchaseReturn.NormalView"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:productPurchaseReturn="clr-namespace:His_Pos.SYSTEM_TAB.H2_STOCK_MANAGE.ProductPurchaseReturn"
             xmlns:orderDetailControl="clr-namespace:His_Pos.SYSTEM_TAB.H2_STOCK_MANAGE.ProductPurchaseReturn.NormalView.OrderDetailControl"
             xmlns:newClass="clr-namespace:His_Pos.NewClass"
             DataContext="{Binding NormalViewModel, UpdateSourceTrigger=PropertyChanged}"
             mc:Ignorable="d">
    <UserControl.Resources>
        <DataTemplate x:Key="PurchaseOrderDetail" DataType="{x:Type productPurchaseReturn:ProductPurchaseReturnViewModel}">
            <orderDetailControl:PurchaseDetailControl/>
        </DataTemplate>
        <DataTemplate x:Key="ReturnOrderDetail" DataType="{x:Type productPurchaseReturn:ProductPurchaseReturnViewModel}">
            <orderDetailControl:ReturnDetailControl/>
        </DataTemplate>
        <DataTemplate x:Key="NoDataDetailControl" DataType="{x:Type productPurchaseReturn:ProductPurchaseReturnViewModel}">
            <orderDetailControl:NoDataDetailControl/>
        </DataTemplate>
        <DataTemplate x:Key="PurchaseOrderIcon" DataType="{x:Type productPurchaseReturn:ProductPurchaseReturnViewModel}">
            <Border BorderBrush="Green" BorderThickness="1" CornerRadius="6" Margin="15,8,1,5">
                <TextBlock Text="進貨" Foreground="Green" FontFamily="Segoe UI Semi" FontSize="15" VerticalAlignment="Center" Margin="5,0,5,0"/>
            </Border>
        </DataTemplate>
        <DataTemplate x:Key="ReturnOrderIcon" DataType="{x:Type productPurchaseReturn:ProductPurchaseReturnViewModel}">
            <Border BorderBrush="Red" BorderThickness="1" CornerRadius="6" Margin="15,8,1,5">
                <TextBlock Text="退貨" Foreground="Red" FontFamily="Segoe UI Semi" FontSize="15" VerticalAlignment="Center" Margin="5,0,5,0"/>
            </Border>
        </DataTemplate>
        <DataTemplate x:Key="UnProcessIcon" DataType="{x:Type productPurchaseReturn:ProductPurchaseReturnViewModel}">
            <Image Source="/Images/OrangeDot.png" Width="20" Margin="10,0,5,0"/>
        </DataTemplate>
        <DataTemplate x:Key="ProcessIcon" DataType="{x:Type productPurchaseReturn:ProductPurchaseReturnViewModel}">
            <Image Source="/Images/BlueDot.png" Width="20" Margin="10,0,5,0"/>
        </DataTemplate>
        <DataTemplate x:Key="WaitingIcon" DataType="{x:Type productPurchaseReturn:ProductPurchaseReturnViewModel}">
            <Image Source="/Images/GreenDot.png" Width="20" Margin="10,0,5,0"/>
        </DataTemplate>
        <DataTemplate x:Key="UnProcessingID" DataType="{x:Type productPurchaseReturn:ProductPurchaseReturnViewModel}">
            <Label Content="{Binding ID}" Width="160" Foreground="Black" FontFamily="Segoe UI Semibold" FontSize="23"/>
        </DataTemplate>
        <DataTemplate x:Key="ProcessingID" DataType="{x:Type productPurchaseReturn:ProductPurchaseReturnViewModel}">
            <Label Content="{Binding ReceiveID}" Width="160" Foreground="Black" FontFamily="Segoe UI Semibold" FontSize="23"/>
        </DataTemplate>
        <newClass:BindingProxy x:Key="proxy" Data="{Binding}" />
    </UserControl.Resources>
    <Grid Width="1365" Height="870">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid Margin="10,10,0,10" Grid.Column="0" Background="{StaticResource GridBackGround}">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="35"/>
                <RowDefinition Height="35"/>
                <RowDefinition Height="50"/>
                <RowDefinition/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>
            <Border Margin="5,5,5,0" CornerRadius="5" Width="260" Grid.Row="0" Background="DimGray"/>
            <StackPanel Orientation="Horizontal" Grid.Row="0" Background="{StaticResource GridBackGround}">
                <Button Content="單張進退貨" Background="DimGray" BorderThickness="0" Height="25" Width="120" Margin="10,10,5,5" FontFamily="Segoe UI Semibold" FontSize="13"/>
                <Button Content="杏德總檔進退貨" Background="Transparent" Foreground="DimGray" BorderThickness="0" Height="25" Width="125" Margin="0,10,5,5" FontFamily="Segoe UI Semibold" FontSize="13"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Grid.Row="1" Background="{StaticResource GridBackGround}">
                <RadioButton Content="全部" Tag="3" GroupName="OrderType" Margin="15,0,5,0" FontSize="18" FontFamily="Segoe UI Semibold" VerticalAlignment="Center" VerticalContentAlignment="Center" IsChecked="True" Command="{Binding FilterOrderCommand}" CommandParameter="4"/>
                <RadioButton Content="等待處理" Tag="4" GroupName="OrderType" Margin="58,0,5,0" FontSize="18" FontFamily="Segoe UI Semibold" VerticalAlignment="Center" VerticalContentAlignment="Center" Command="{Binding FilterOrderCommand}" CommandParameter="2"/>
                <Image Source="/Images/GreenDot.png" Width="15"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Grid.Row="2" Background="{StaticResource GridBackGround}">
                <RadioButton Content="採購待退" Tag="0" GroupName="OrderType" Margin="15,0,5,0" FontSize="18" FontFamily="Segoe UI Semibold" VerticalAlignment="Center" VerticalContentAlignment="Center" Command="{Binding FilterOrderCommand}" CommandParameter="1"/>
                <Image Source="/Images/OrangeDot.png" Width="15"/>
                <RadioButton Content="進退貨單" Tag="1" GroupName="OrderType" Margin="7,0,5,0" FontSize="18" FontFamily="Segoe UI Semibold" VerticalAlignment="Center" VerticalContentAlignment="Center" Command="{Binding FilterOrderCommand}" CommandParameter="3"/>
                <Image Source="/Images/BlueDot.png" Width="15"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Grid.Row="3" Background="{StaticResource GridBackGround}" Margin="0,2,0,0">
                <TextBox Text="{Binding SearchString, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" FontSize="18" FontFamily="Segoe UI Semibold" VerticalAlignment="Center" HorizontalContentAlignment="Center" Width="260" Margin="5" materialDesign:HintAssist.Hint="篩選條件 (單號 處方 備註)">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="TextChanged">
                            <i:InvokeCommandAction Command="{Binding FilterOrderCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </TextBox>
            </StackPanel>
            <DataGrid Grid.Row="4" Background="Transparent" HeadersVisibility="None" HorizontalScrollBarVisibility="Hidden" GridLinesVisibility="None" ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                      AutoGenerateColumns="False" VerticalScrollBarVisibility="Hidden" CanUserAddRows="False" ItemsSource="{Binding StoreOrderCollectionView}" CanUserDeleteRows="False"
                      SelectedItem="{Binding CurrentStoreOrder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectionChanged="StoreOrders_OnSelectionChanged">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{StaticResource GridSelected}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource Shadow}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTemplateColumn Width="270">
                        <DataGridTemplateColumn.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderBrush" Value="Transparent"/>
                            </Style>
                        </DataGridTemplateColumn.CellStyle>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid Height="70">
                                    <Grid.ContextMenu>
                                        <ContextMenu TextBlock.TextAlignment="Center">
                                            <MenuItem Header="匯出選取訂單資料" Command="{Binding Data.ExportOrderDataCommand, Source={StaticResource proxy}}" CommandParameter="S"/>
                                        </ContextMenu>
                                    </Grid.ContextMenu>
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="40"/>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>
                                    <ContentControl Grid.Column="0" Grid.Row="0" Grid.RowSpan="2">
                                        <ContentControl.Style>
                                            <Style TargetType="{x:Type ContentControl}">
                                                <Setter Property="ContentTemplate" Value="{StaticResource UnProcessIcon}" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding OrderStatus, UpdateSourceTrigger=PropertyChanged}" Value="SINGDE_UNPROCESSING">
                                                        <Setter Property="ContentTemplate" Value="{StaticResource UnProcessIcon}" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding OrderStatus, UpdateSourceTrigger=PropertyChanged}" Value="NORMAL_PROCESSING">
                                                        <Setter Property="ContentTemplate" Value="{StaticResource ProcessIcon}" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding OrderStatus, UpdateSourceTrigger=PropertyChanged}" Value="SINGDE_PROCESSING">
                                                        <Setter Property="ContentTemplate" Value="{StaticResource ProcessIcon}" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding OrderStatus, UpdateSourceTrigger=PropertyChanged}" Value="WAITING">
                                                        <Setter Property="ContentTemplate" Value="{StaticResource WaitingIcon}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ContentControl.Style>
                                    </ContentControl>
                                    <StackPanel Orientation="Horizontal" Grid.Column="1" Grid.Row="0">
                                        <ContentControl Content="{Binding}">
                                            <ContentControl.Style>
                                                <Style TargetType="{x:Type ContentControl}">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource UnProcessingID}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding OrderStatus, UpdateSourceTrigger=PropertyChanged}" Value="NORMAL_PROCESSING">
                                                            <Setter Property="ContentTemplate" Value="{StaticResource ProcessingID}" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding OrderStatus, UpdateSourceTrigger=PropertyChanged}" Value="SINGDE_PROCESSING">
                                                            <Setter Property="ContentTemplate" Value="{StaticResource ProcessingID}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ContentControl.Style>
                                        </ContentControl>
                                        <ContentControl>
                                            <ContentControl.Style>
                                                <Style TargetType="{x:Type ContentControl}">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource PurchaseOrderIcon}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding OrderType}" Value="RETURN">
                                                            <Setter Property="ContentTemplate" Value="{StaticResource ReturnOrderIcon}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ContentControl.Style>
                                        </ContentControl>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Grid.Column="1" Grid.Row="1">
                                        <Label Content="{Binding OrderWarehouse.Name}" Foreground="RoyalBlue" Width="45" FontFamily="Segoe UI Semi" FontSize="14" VerticalAlignment="Center"/>
                                        <Label Content="{Binding OrderManufactory.GetName}" Foreground="{StaticResource ForeGround}" Width="60" FontFamily="Segoe UI Semibold" FontSize="14" VerticalAlignment="Center"/>
                                        <Label Content="{Binding ProductCount, UpdateSourceTrigger=PropertyChanged}" Foreground="{StaticResource ForeGround}" FontFamily="Segoe UI Semi" Width="50" FontSize="14" VerticalAlignment="Center" HorizontalContentAlignment="Right" Margin="10,0,0,0"/>
                                        <Label Content="個品項" Foreground="{StaticResource ForeGround}" FontFamily="Segoe UI Semi" FontSize="14" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            <StackPanel Orientation="Horizontal" Grid.Row="5" Background="{StaticResource GridBackGround}">
                <Image Source="/Images/Plus.png" Width="35" Margin="10,0,0,0">
                    <Image.InputBindings>
                        <MouseBinding Command="{Binding AddOrderCommand}" MouseAction="LeftClick"/>
                    </Image.InputBindings>
                </Image>
                <Rectangle Width="185"/>
                <Image Source="/Images/reload.png" Width="25" Margin="5,0,0,0">
                    <Image.InputBindings>
                        <MouseBinding Command="{Binding ReloadCommand}" MouseAction="LeftClick"/>
                    </Image.InputBindings>
                </Image>
            </StackPanel>
        </Grid>
        <ContentControl Grid.Column="1" Margin="0,10,10,10">
            <ContentControl.Style>
                <Style TargetType="{x:Type ContentControl}">
                    <Setter Property="ContentTemplate" Value="{StaticResource NoDataDetailControl}" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Path=CurrentStoreOrder.OrderType}" Value="RETURN">
                            <Setter Property="ContentTemplate" Value="{StaticResource ReturnOrderDetail}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=CurrentStoreOrder.OrderType}" Value="PURCHASE">
                            <Setter Property="ContentTemplate" Value="{StaticResource PurchaseOrderDetail}" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ContentControl.Style>
        </ContentControl>
    </Grid>
</UserControl>
