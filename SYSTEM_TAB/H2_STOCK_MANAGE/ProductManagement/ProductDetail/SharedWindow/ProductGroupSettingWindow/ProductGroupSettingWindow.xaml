<Window x:Class="His_Pos.SYSTEM_TAB.H2_STOCK_MANAGE.ProductManagement.ProductDetail.SharedWindow.ProductGroupSettingWindow.ProductGroupSettingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:productgroupsetting="clr-namespace:His_Pos.SYSTEM_TAB.H2_STOCK_MANAGE.ProductManagement.ProductDetail.SharedWindow.ProductGroupSettingWindow.ProductGroupSettingUsercontrol"
        xmlns:local="clr-namespace:His_Pos.SYSTEM_TAB.H2_STOCK_MANAGE.ProductManagement.ProductDetail.SharedWindow.ProductGroupSettingWindow"
        mc:Ignorable="d"
        Title="商品群組設定" Height="400" Width="450" >
    
    <Window.Resources>
        <DataTemplate x:Key="MergeProduct" DataType="{x:Type productgroupsetting:MergeProductUserControl}">
            <productgroupsetting:MergeProductUserControl/>
        </DataTemplate>
        <DataTemplate x:Key="SplitProduct" DataType="{x:Type productgroupsetting:SplitProductUserControl}">
            <productgroupsetting:SplitProductUserControl/>
        </DataTemplate>
    </Window.Resources>
    <Grid  Background="{StaticResource GridDarkestBackGround}">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <RadioButton Content="併庫" Command="{Binding SetIsSpiltFalseCommand}"  IsChecked="True"  Margin="15,0,5,0" FontSize="18" FontFamily="Segoe UI Semibold" VerticalAlignment="Center" VerticalContentAlignment="Center"/>
            <RadioButton Content="拆庫" Command="{Binding SetIsSpiltTrueCommand}" Margin="10,0,5,0" FontSize="18" FontFamily="Segoe UI Semibold" VerticalAlignment="Center" VerticalContentAlignment="Center"  />
             </StackPanel>
        <ContentControl Grid.Row="1" Content="{Binding}">
            <ContentControl.Style>
                <Style TargetType="{x:Type ContentControl}">
                    <Setter Property="ContentTemplate" Value="{StaticResource MergeProduct}" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsSplit,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Value="True">
                            <Setter Property="ContentTemplate" Value="{StaticResource SplitProduct}"/>
                        </DataTrigger>
                    </Style.Triggers> 
                </Style>
            </ContentControl.Style>
        </ContentControl>
    </Grid>
</Window>
