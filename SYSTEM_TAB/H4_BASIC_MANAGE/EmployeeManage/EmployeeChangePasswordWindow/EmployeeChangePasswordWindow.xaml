<Window x:Class="His_Pos.SYSTEM_TAB.H4_BASIC_MANAGE.EmployeeManage.EmployeeChangePasswordWindow.EmployeeChangePasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:His_Pos.SYSTEM_TAB.H4_BASIC_MANAGE.EmployeeManage.EmployeeChangePasswordWindow"
        mc:Ignorable="d"
        Title="員工密碼修改" Height="230" Width="250" WindowStartupLocation="CenterScreen"> 
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="5,0,0,0">
            <Label Content="舊密碼" Foreground="Black" FontSize="18" FontFamily="Segoe UI Semibold" VerticalAlignment="Center" Padding="0"/>
            <TextBox Text="{Binding OldPassword,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="170" VerticalAlignment="Center" Foreground="Black" FontSize="18" FontFamily="Segoe UI Semibold" Margin="5,0,0,0"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="5,0,0,0">
            <Label Content="新密碼" Foreground="Black" FontSize="18" FontFamily="Segoe UI Semibold" VerticalAlignment="Center" Padding="0"/>
            <TextBox Text="{Binding NewPassword,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="170" VerticalAlignment="Center" Foreground="Black" FontSize="18" FontFamily="Segoe UI Semibold" Margin="5,0,0,0"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="5,0,0,0">
            <Label Content="確認密碼" Foreground="Black" FontSize="18" FontFamily="Segoe UI Semibold" VerticalAlignment="Center" Padding="0"/>
            <TextBox Text="{Binding  ConfirmPassword,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="150" VerticalAlignment="Center" Foreground="Black" FontSize="18" FontFamily="Segoe UI Semibold" Margin="5,0,0,0"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="3" Margin="5,0,0,0">
            <Button Width="100" Command="{Binding SubmitCommand}" Margin="130,0,0,0" Content="修改" Height="30" FontSize="18" FontFamily="Segoe UI Semibold" Background="RoyalBlue" BorderThickness="0" />
        </StackPanel>
    </Grid>
</Window>
