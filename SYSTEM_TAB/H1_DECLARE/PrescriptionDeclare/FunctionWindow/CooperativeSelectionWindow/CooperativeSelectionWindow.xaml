<Window x:Class="His_Pos.SYSTEM_TAB.H1_DECLARE.PrescriptionDeclare.FunctionWindow.CooperativeSelectionWindow.CooperativeSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:service="clr-namespace:His_Pos.Service"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:command="http://www.galasoft.ch/mvvmlight"
        mc:Ignorable="d" Title="合作診所查詢" 
        FontFamily="Segoe UI Semibold" Height="630" Width="1000"
        x:Name="CooperativeSelectWindow"
        WindowStartupLocation="CenterScreen" >
    <Window.Resources>
        <service:NullableDateConverter x:Key="NullableDateConverter" />
        <service:DateConverter x:Key="DateConverter" />
    </Window.Resources>
    <!--<Window.DataContext>
        <local:CooperativeSelectionViewModel/>
    </Window.DataContext>-->
    <Viewbox Stretch="Fill">
        <xctk:BusyIndicator IsBusy="{Binding IsBusy, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" BusyContent="{Binding BusyContent, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
                <Label Content="開始日期" Margin="0,5,0,0" Style="{StaticResource BoldLabelContent18}"/>
                <xctk:MaskedTextBox Name="Start" Width="100" HorizontalContentAlignment="Center" FontSize="18" Style="{StaticResource MaterialDesignTextBox}"
                         Foreground="{StaticResource ForeGround}" Mask="000/00/00" PromptChar="-" ValueDataType="xctk:DateTimeFormat" InsertKeyMode="Overwrite">
                    <xctk:MaskedTextBox.Text>
                        <Binding Path="StartDate" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged"
                                 ValidatesOnDataErrors="True" NotifyOnValidationError="True" Converter="{StaticResource NullableDateConverter}">
                            <Binding.ValidationRules>
                                <service:DateValidationRule/>
                            </Binding.ValidationRules>
                        </Binding>
                    </xctk:MaskedTextBox.Text>
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="TextChanged">
                            <command:EventToCommand
                                Command="{Binding StartDateChanged}"
                                CommandParameter="{Binding Text, ElementName=Start}" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </xctk:MaskedTextBox>
                <Label Content="結束日期" Margin="10,5,0,0" Style="{StaticResource BoldLabelContent18}"/>
                <xctk:MaskedTextBox Name="End" Width="100" Margin="5,0,0,0" HorizontalContentAlignment="Center"  FontSize="18" Style="{StaticResource MaterialDesignTextBox}"
                         Foreground="{StaticResource ForeGround}" Mask="000/00/00" PromptChar="-" ValueDataType="xctk:DateTimeFormat" InsertKeyMode="Overwrite">
                    <xctk:MaskedTextBox.Text>
                        <Binding Path="EndDate" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged" 
                                 ValidatesOnDataErrors="True" NotifyOnValidationError="True" Converter="{StaticResource NullableDateConverter}">
                            <Binding.ValidationRules>
                                <service:DateValidationRule/>
                            </Binding.ValidationRules>
                        </Binding>
                    </xctk:MaskedTextBox.Text>
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="TextChanged">
                            <command:EventToCommand
                                Command="{Binding EndDateChanged}"
                                CommandParameter="{Binding Text, ElementName=End}" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </xctk:MaskedTextBox>
                <Rectangle Width="395"/>
                <Button Name="Back" Width="35" Height="35" Background="Transparent" BorderBrush="Transparent" Margin="120,0,0,0" Padding="0" Command="{Binding Refresh}">
                    <Image Source="/Images/refresh.png" Height="35" Width="35" Margin="0"/>
                </Button>
                <Button Content="列印藥袋" Background="RoyalBlue" Width="100" BorderThickness="0" FontSize="16" Margin="10,0,0,0" Command="{Binding PrintMedBag}"/>
            </StackPanel>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="450"/>
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="475"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="10" Background="{StaticResource GridBackGround}">
                        <TextBox Name="IdNumberText" Width="170" Height="30" Margin="10,0,0,0" FontSize="18" HorizontalContentAlignment="Center" materialDesign:HintAssist.Hint="請輸入身分證" Text="{Binding IDNumber,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
                        <CheckBox Name="NotReadCheckBox" BorderBrush="DimGray" Content="未讀" IsChecked="{Binding IsNotRead,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" FontSize="20" Foreground="DimGray" VerticalAlignment="Center" Padding="0" Margin="0,0,0,0" Background="RoyalBlue"/>
                        <CheckBox Name="IsReadCheckBox" BorderBrush="DimGray" Content="已讀" IsChecked="{Binding IsRead,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" FontSize="20" Foreground="DimGray" VerticalAlignment="Center" Padding="0" Margin="10,0,0,0" Background="RoyalBlue"/>
                            <Label Content="處方量" Foreground="DimGray" FontSize="18" FontFamily="Segoe UI Semibold" VerticalAlignment="Center" Padding="0" Margin="30,0,0,0"/>
                            <Label Content="{Binding PrescriptionCount,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Foreground="DimGray" FontSize="18" FontFamily="Segoe UI Semibold" VerticalAlignment="Center" Padding="0"/>
                     </StackPanel>
                    <DataGrid Name="PrescriptionsGrid" Grid.Row="1" HeadersVisibility="None" HorizontalScrollBarVisibility="Hidden" Margin="10,0,10,10" Background="{StaticResource GridDarkerBackGround}" 
                              AutoGenerateColumns="False" VerticalScrollBarVisibility="Hidden" CanUserAddRows="False" ItemsSource="{Binding CooPreCollectionView,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}" 
                              ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}" SelectedItem="{Binding SelectedPrescription,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="MouseDoubleClick">
                                <i:InvokeCommandAction Command="{Binding PrescriptionSelected}"/>
                            </i:EventTrigger>
                            <i:EventTrigger EventName="SelectionChanged">
                                <i:InvokeCommandAction Command="{Binding SelectionChanged}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        <DataGrid.RowStyle>
                            <Style TargetType="{x:Type DataGridRow}">
                                <Style.Triggers>
                                    <Trigger Property="DataGridRow.IsSelected" Value="True">
                                        <Setter Property="Background" Value="{StaticResource GridSelected}"/>
                                    </Trigger>
                                </Style.Triggers>
                                <Setter Property="Background" Value="Transparent"/>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn>
                                <DataGridTemplateColumn.CellStyle>
                                    <Style TargetType="{x:Type DataGridCell}">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="BorderBrush" Value="Transparent"/>
                                        <Setter Property="Foreground" Value="{StaticResource ForeGround}"/>
                                    </Style>
                                </DataGridTemplateColumn.CellStyle>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <StackPanel Orientation="Horizontal" Grid.Row="0">
                                                <Label Content="{Binding Patient.Name}" Width="130" FontSize="25" HorizontalContentAlignment="Center" VerticalAlignment="Center"/>
                                                <Label Content="{Binding Patient.IDNumber}" FontSize="20" VerticalAlignment="Center"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Grid.Row="1">
                                                <Label Content="{Binding Treatment.Institution.Name}" Width="130" FontSize="15" HorizontalContentAlignment="Center" VerticalAlignment="Center"/>
                                                <Label Content="{Binding Treatment.Division.Name}" Width="100" FontSize="15" HorizontalContentAlignment="Center" VerticalAlignment="Center"/>
                                                <Label Content="{Binding Treatment.TreatDate,Converter={StaticResource DateConverter}}" FontSize="15" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Label Grid.Row="0" Content="顧客處方紀錄" Style="{StaticResource BoldLabelContent18}" />
                    <DataGrid  Name="CustomerHistoryGrid" Grid.Row="1" Margin="0,0,10,10" AutoGenerateColumns="False" Background="{StaticResource GridDarkerBackGround}" MaxHeight="228"
                        CanUserAddRows="False" CellStyle="{StaticResource DataGridCellStyle}" ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}" GridLinesVisibility="Horizontal"
                        ItemsSource="{Binding CustomerHistories,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedHistory,UpdateSourceTrigger=PropertyChanged}">
                        <DataGrid.RowStyle>
                            <Style TargetType="{x:Type DataGridRow}">
                                <Style.Triggers>
                                    <Trigger Property="DataGridRow.IsSelected" Value="True">
                                        <Setter Property="Background" Value="{StaticResource GridDarkerBackGround}" />
                                    </Trigger>
                                </Style.Triggers>
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderBrush" Value="Transparent" />
                                <Setter Property="FontSize" Value="16" />
                                <Setter Property="Height" Value="40" />
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Width="30"/>
                            <DataGridTextColumn Width="250" Binding="{Binding Institution,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}" 
                                                FontSize="14" FontWeight="Bold" Header="釋出院所" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="{x:Type TextBlock}">
                                        <Setter Property="HorizontalAlignment" Value="Center" />
                                        <Setter Property="VerticalAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Width="100" Binding="{Binding Division,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}"
                                                FontSize="14" FontWeight="Bold" Header="科別" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="{x:Type TextBlock}">
                                        <Setter Property="HorizontalAlignment" Value="Center" />
                                        <Setter Property="VerticalAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Width="100" Binding="{Binding AdjustDate,Mode=OneWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource DateConverter}}"
                           FontSize="14" FontWeight="Bold"  Header="調劑日" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="{x:Type TextBlock}">
                                        <Setter Property="HorizontalAlignment" Value="Center" />
                                        <Setter Property="VerticalAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Width="100" Binding="{Binding TotalPoint,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}"
                           FontSize="14" FontWeight="Bold"  Header="點數" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="{x:Type TextBlock}">
                                        <Setter Property="HorizontalAlignment" Value="Center" />
                                        <Setter Property="VerticalAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    <DataGrid  Grid.Row="2" AutoGenerateColumns="False" Background="{StaticResource GridDarkerBackGround}" CanUserAddRows="False" CellStyle="{StaticResource DataGridCellStyle}" Margin="0,0,10,10" DataContext="{Binding SelectedHistory}" MaxHeight="227"
                            ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}" GridLinesVisibility="Horizontal" ItemsSource="{Binding Products,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}" >
                        <DataGrid.RowStyle>
                            <Style TargetType="{x:Type DataGridRow}">
                                <Style.Triggers>
                                    <Trigger Property="DataGridRow.IsSelected" Value="True">
                                        <Setter Property="Background" Value="{StaticResource GridDarkerBackGround}" />
                                    </Trigger>
                                </Style.Triggers>
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderBrush" Value="Transparent" />
                                <Setter Property="FontSize" Value="16" />
                                <Setter Property="Height" Value="40" />
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Width="100"
                                     Binding="{Binding ID,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}"   FontSize="14" FontWeight="Bold" Header="商品編號" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="{x:Type TextBlock}">
                                        <Setter Property="HorizontalAlignment" Value="Center" />
                                        <Setter Property="VerticalAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Width="250" Binding="{Binding FullName,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}" FontSize="14" FontWeight="Bold"  Header="商品名稱" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="{x:Type TextBlock}">
                                        <Setter Property="HorizontalAlignment" Value="Left" />
                                        <Setter Property="VerticalAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Width="80" Binding="{Binding Dosage,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}" FontSize="14" FontWeight="Bold"  Header="用量" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="{x:Type TextBlock}">
                                        <Setter Property="HorizontalAlignment" Value="Center" />
                                        <Setter Property="VerticalAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Width="80" Binding="{Binding UsageName,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}"
                                                FontSize="14" FontWeight="Bold"  Header="用法" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="{x:Type TextBlock}">
                                        <Setter Property="HorizontalAlignment" Value="Center" />
                                        <Setter Property="VerticalAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Width="80" Binding="{Binding Amount,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}"
                                                FontSize="14" FontWeight="Bold"  Header="總量" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="{x:Type TextBlock}">
                                        <Setter Property="HorizontalAlignment" Value="Center" />
                                        <Setter Property="VerticalAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Grid>
        </Grid>
        </xctk:BusyIndicator>
    </Viewbox>
</Window>
