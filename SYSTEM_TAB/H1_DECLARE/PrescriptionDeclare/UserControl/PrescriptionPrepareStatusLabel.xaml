<UserControl x:Class="His_Pos.SYSTEM_TAB.H1_DECLARE.PrescriptionDeclare.UserControl.PrescriptionPrepareStatusLabel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:His_Pos.SYSTEM_TAB.H1_DECLARE.PrescriptionDeclare.UserControl"
             x:Name="Root"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style  x:Key="MyLabelStyle" TargetType="Label">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Label}">
                        <Border BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}" 
                                SnapsToDevicePixels="true" 
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Label Height="35" FontSize="16" FontFamily="Segoe UI Semibold" Style="{StaticResource MyLabelStyle}" BorderThickness="2" Content="{Binding CurrentPrescription.PrescriptionStatus.OrderStatus,UpdateSourceTrigger=PropertyChanged}" BorderBrush="{Binding MyBrush,UpdateSourceTrigger=PropertyChanged,ElementName=Root}"
               Foreground="{Binding MyBrush,UpdateSourceTrigger=PropertyChanged,ElementName=Root}" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Width="{Binding LabelWidth,UpdateSourceTrigger=PropertyChanged,ElementName=Root}"/>
</UserControl>
