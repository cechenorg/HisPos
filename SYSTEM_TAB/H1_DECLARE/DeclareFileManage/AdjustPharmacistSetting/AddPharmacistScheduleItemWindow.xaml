<Window x:Class="His_Pos.SYSTEM_TAB.H1_DECLARE.DeclareFileManage.AdjustPharmacistSetting.AddPharmacistScheduleItemWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:His_Pos.SYSTEM_TAB.H1_DECLARE.DeclareFileManage.AdjustPharmacistSetting"
        mc:Ignorable="d"
        Title="新增藥師" Height="140" Width="350" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Grid.Row="0">
            <Label Height="35" Margin="10" HorizontalAlignment="Left" Width="85" FontSize="18" Content="選擇藥師:" Style="{StaticResource BoldLabelContent16}"/>
            <ComboBox Width="120" ItemsSource="{Binding MedicalPersonnels}" SelectedItem="{Binding SelectedMedicalPersonnel,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" FontSize="16" FontFamily="Segoe UI SemiBold" HorizontalAlignment="Center"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Row="1" Margin="0,0,0,0" HorizontalAlignment="Right">
            <Button Margin="0,0,10,0" Width="80" VerticalAlignment="Bottom" Background="RoyalBlue" BorderBrush="RoyalBlue" Command="{Binding Save}" Content="新增" FontSize="16" FontFamily="Segoe UI SemiBold"/>
            <Button HorizontalAlignment="Right" Margin="0,0,10,0" VerticalAlignment="Bottom" Width="75" Background="DimGray" BorderBrush="DimGray" Command="{Binding Cancel}" Content="取消" FontSize="16" FontFamily="Segoe UI SemiBold"/>
        </StackPanel>
    </Grid>
</Window>
