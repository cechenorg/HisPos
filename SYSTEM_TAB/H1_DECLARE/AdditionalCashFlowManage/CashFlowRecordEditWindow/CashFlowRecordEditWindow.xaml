<Window x:Class="His_Pos.SYSTEM_TAB.H1_DECLARE.AdditionalCashFlowManage.CashFlowRecordEditWindow.CashFlowRecordEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:His_Pos.SYSTEM_TAB.H1_DECLARE.AdditionalCashFlowManage.CashFlowRecordEditWindow"
        mc:Ignorable="d"
        Title="額外收支編輯" Height="305" Width="670" FontFamily="Segoe UI Semibold">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="55"/>
            <RowDefinition Height="55"/>
            <RowDefinition Height="55"/>
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Vertical" Grid.Row="0">
            <Label Content="原始資料 : " FontSize="20" Margin="10,0,0,0"/>
            <Label Content="{Binding OriginContent,UpdateSourceTrigger=PropertyChanged}" FontSize="18" Margin="20,0,0,0"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1" >
            <Label Content="收入/支出 :" FontSize="20" VerticalAlignment="Center" Margin="10,0,0,0"/>
            <RadioButton Content="收入" Tag="3" Margin="15,0,5,0" FontSize="18" FontFamily="Segoe UI Semibold" VerticalAlignment="Center" VerticalContentAlignment="Center" IsChecked="{Binding IncomeCheck,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
            <RadioButton Content="支出" Tag="4" Margin="10,0,5,0" FontSize="18" FontFamily="Segoe UI Semibold" VerticalAlignment="Center" VerticalContentAlignment="Center" IsChecked="{Binding ExpensesCheck,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
            <Label Content="科目 : " FontSize="20" VerticalAlignment="Center" Margin="20,0,0,0"/>
            <ComboBox Width="100" Margin="10,0,0,0" FontSize="18" ItemsSource="{Binding CashFlowAccounts,UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedCashFlowAccount,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" HorizontalContentAlignment="Center" />
            <Label Content="金額 : " FontSize="20" VerticalAlignment="Center" Margin="20,0,0,0"/>
            <TextBox Width="80" Text="{Binding CashFlowValue,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" FontSize="18" Margin="10,0,0,0" TextAlignment="Center" InputScope="Number"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="2" >
            <Label Content="備註 : " FontSize="20" VerticalAlignment="Center" Margin="10,0,0,0"/>
            <TextBox Width="555" Text="{Binding EditedCashFlowRecord.Note,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" FontSize="18" Margin="10,0,0,0" TextAlignment="Left"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Right">
            <Button Content="取消" Background="Gray" BorderBrush="Transparent" FontSize="18" Command="{Binding Cancel}"/>
            <Button Content="確認" Background="IndianRed" BorderBrush="Transparent" FontSize="18" Margin="10" Command="{Binding Submit}"/>
        </StackPanel>
    </Grid>
</Window>
