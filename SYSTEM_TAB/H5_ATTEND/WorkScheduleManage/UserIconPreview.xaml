<UserControl x:Class="His_Pos.SYSTEM_TAB.H5_ATTEND.WorkScheduleManage.UserIconPreview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:workScheduleManage="clr-namespace:His_Pos.SYSTEM_TAB.H5_ATTEND.WorkScheduleManage"
             mc:Ignorable="d" 
             d:DesignHeight="45" d:DesignWidth="100">
    <UserControl.Resources>
        <workScheduleManage:IsMedConverter x:Key="IsMedConverter"/>
        <workScheduleManage:IsSelectedConverter x:Key="IsSelectedConverter"/>
    </UserControl.Resources>
    <Border Margin="20,0,0,8" CornerRadius="22" Background="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource IsSelectedConverter}}">
        <StackPanel Orientation="Horizontal">
            <Ellipse Name="UserColor" Width="20" Height="20" Stroke="Red" StrokeThickness="{Binding IsMed, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource IsMedConverter}}" Margin="10,0,5,0"/>
            <TextBlock Name="UserName" Foreground="{StaticResource ForeGround}" VerticalAlignment="Center" FontSize="18" FontFamily="Segoe UI Semibold" Margin="5,0,10,0"/>
        </StackPanel>
    </Border>
</UserControl>
