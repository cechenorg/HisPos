<UserControl x:Class="His_Pos.H4_BASIC_MANAGE.MedFrequencyManage.MedFrequencyManageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:His_Pos.H4_BASIC_MANAGE.MedFrequencyManage"
              xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"   GotFocus="UserControl_GotFocus"
            d:DesignHeight="839" d:DesignWidth="1365">
    <Viewbox Stretch="Fill">
        <Grid Width="1365" Height="839">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition />
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="0" Margin="9" Grid.ColumnSpan="2">
                <Label Content="頻率代號" Foreground="{StaticResource ForeGround}" Margin="10,0,10,0" FontSize="18" FontFamily="Segoe UI Semibold"/>
                <TextBox Margin="0,0,10,0" Width="130" FontSize="16"/>
                <Rectangle Width="990"/>
                <Button Name="ButtonSearch" Content="查詢" Background="DeepSkyBlue" Height="30" FontSize="18" FontWeight="Bold" Width="100" BorderThickness="0"/>
            </StackPanel>
            <DataGrid Name="DataGridMedFrequency" Grid.Row="1" Grid.Column="0" Background="{StaticResource GridBackGround}" ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                           SelectionChanged="DataGridMedFrequency_SelectionChanged" ItemsSource="{Binding UsageCollection,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                           CellStyle="{StaticResource DataGridCellStyle}" CanUserAddRows="False" AutoGenerateColumns="False" Margin="10,0,0,0" HeadersVisibility="None"
                           HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Hidden">
                <DataGrid.RowStyle>
                    <Style TargetType="{x:Type DataGridRow}">
                        <Style.Triggers>
                            <Trigger Property="DataGridRow.IsSelected" Value="True">
                                <Setter Property="Background" Value="{StaticResource GridSelected}"/>
                            </Trigger>
                        </Style.Triggers>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="Height" Value="80"/>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTemplateColumn>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="45"/>
                                        <RowDefinition Height="35"/>
                                    </Grid.RowDefinitions>
                                    <Label FontSize="25" Grid.Row="0" FontFamily="Segoe UI Semibold" Content="{Binding Name,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Margin="10,0,0,0"/>
                                    <Label FontSize="16" Grid.Row="1" FontFamily="Segoe UI Semi" Foreground="{StaticResource ForeGround}" Content="{Binding PrintName,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Margin="20,0,0,0"/>
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            <Grid Grid.Row="1" Grid.Column="1" Grid.RowSpan="2" Background="{StaticResource GridDarkerBackGround}" Margin="0,0,10,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="80"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition/>
                    <RowDefinition Height="50"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.ColumnSpan="4">
                    <TextBox Text="{Binding UsageDetail.Name,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="代號" BorderThickness="0" TextChanged="TextBox_DataChanged" Width="250" VerticalAlignment="Center" Foreground="Black" FontSize="40" FontWeight="Bold" FontFamily="Segoe UI Semibold" Margin="10,0,0,0"/>
                    <TextBox Text="{Binding UsageDetail.PrintName,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="名稱" BorderThickness="0" TextChanged="TextBox_DataChanged" Width="750" VerticalAlignment="Center" Foreground="Black" FontSize="40" FontWeight="Bold" FontFamily="Segoe UI Semibold" Margin="30,0,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="0" Margin="8">
                    <Label  Content="頻率簡碼" FontFamily="Segoe UI Semibold" FontSize="20" VerticalAlignment="Center"/>
                    <TextBox Width="140" Text="{Binding UsageDetail.QuickName,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" TextChanged="TextBox_DataChanged" FontFamily="Segoe UI Semi" FontSize="18" HorizontalContentAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="1" Margin="8">
                    <Label  Content="使用天數" FontFamily="Segoe UI Semibold" FontSize="20" VerticalAlignment="Center"/>
                    <TextBox Width="140" Text="{Binding UsageDetail.Days,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" TextChanged="TextBox_DataChanged" FontFamily="Segoe UI Semi" FontSize="18" HorizontalContentAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="2" Margin="8">
                    <Label  Content="使用次數" FontFamily="Segoe UI Semibold" FontSize="20" VerticalAlignment="Center"/>
                    <TextBox Width="140" Text="{Binding UsageDetail.Times,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" TextChanged="TextBox_DataChanged" FontFamily="Segoe UI Semi" FontSize="18" HorizontalContentAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="4" Background="{StaticResource Shadow}">
                    <Label  Content="藥袋勾選" FontFamily="Segoe UI Semibold" FontSize="20" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    <CheckBox Content="早上" FontSize="20" FontFamily="Segoe UI Semibold" VerticalAlignment="Center" Background="DimGray" Margin="15,0,0,0" IsChecked="{Binding UsageDetail.PrintIcons[0], Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Click="CheckBox_DataChanged"/>
                    <CheckBox Content="中午" FontSize="20" FontFamily="Segoe UI Semibold" VerticalAlignment="Center" Background="DimGray" Margin="30,0,0,0" IsChecked="{Binding UsageDetail.PrintIcons[1], Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Click="CheckBox_DataChanged"/>
                    <CheckBox Content="晚上" FontSize="20" FontFamily="Segoe UI Semibold" VerticalAlignment="Center" Background="DimGray" Margin="30,0,0,0" IsChecked="{Binding UsageDetail.PrintIcons[2], Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Click="CheckBox_DataChanged"/>
                    <CheckBox Content="睡前" FontSize="20" FontFamily="Segoe UI Semibold" VerticalAlignment="Center" Background="DimGray" Margin="30,0,0,0" IsChecked="{Binding UsageDetail.PrintIcons[3], Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Click="CheckBox_DataChanged"/>
                    <CheckBox Content="飯前" FontSize="20" FontFamily="Segoe UI Semibold" VerticalAlignment="Center" Background="DimGray" Margin="30,0,0,0" IsChecked="{Binding UsageDetail.PrintIcons[4], Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Click="CheckBox_DataChanged"/>
                    <CheckBox Content="飯後" FontSize="20" FontFamily="Segoe UI Semibold" VerticalAlignment="Center" Background="DimGray" Margin="30,0,0,0" IsChecked="{Binding UsageDetail.PrintIcons[5], Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Click="CheckBox_DataChanged"/>
                </StackPanel>
                <StackPanel  Orientation="Horizontal"  Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="4" Background="{StaticResource GridDarkerBackGround}">
                    <Rectangle Width="670"/>
                    <Label Name="Changed" Width="100"  Grid.Row="4" Grid.Column="1" HorizontalContentAlignment="Center" VerticalAlignment="Center" FontFamily="Segoe UI Semibold" FontSize="20"/>
                    <Button Name="ButtonCancel" Width="120" Content="取消" FontFamily="Segoe UI Semibold" Click="ButtonCancel_Click" FontSize="16" Background="DimGray" BorderThickness="0" Margin="20,0,0,0"/>
                    <Button Name="ButtonSubmit" Width="120" Content="確認" FontFamily="Segoe UI Semibold" Click="ButtonSubmit_Click" FontSize="16" Background="Red" BorderThickness="0" Margin="10,0,0,0"/>
                </StackPanel>
                
            </Grid>
            <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="0" Background="{StaticResource GridBackGround}" Margin="10,0,0,10">
                <Image Source="/Images/Plus.png" Width="35" Margin="10,0,0,0" MouseLeftButtonDown="Image_MouseLeftButtonDown"/>
                <Rectangle Width="210"/>
                <Image Source="/Images/Delete.png" Width="30" Name="ButtonDelete" MouseLeftButtonDown="ButtonDelete_MouseLeftButtonDown"/>
            </StackPanel>
        </Grid>
    </Viewbox>
</UserControl>
