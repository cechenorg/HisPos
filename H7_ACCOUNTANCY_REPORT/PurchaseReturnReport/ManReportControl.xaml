<UserControl x:Class="His_Pos.H7_ACCOUNTANCY_REPORT.PurchaseReturnReport.ManReportControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:His_Pos.H7_ACCOUNTANCY_REPORT.PurchaseReturnReport"
             xmlns:componentmodel="clr-namespace:System.ComponentModel;assembly=WindowsBase"
             mc:Ignorable="d" Width="660">
    <UserControl.Resources>
        <Style x:Key="IconSelectedStyle" TargetType="Border">
            <Setter Property="Background" Value="LightBlue"/>
            <Setter Property="BorderBrush" Value="RoyalBlue"/>
            <Setter Property="BorderThickness" Value="2"/>
        </Style>
        <Style x:Key="IconSelectingStyle" TargetType="Border">
            <Setter Property="Background" Value="LightBlue"/>
            <Setter Property="BorderBrush" Value="RoyalBlue"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style x:Key="IconUnSelectedStyle" TargetType="Border">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="RoyalBlue"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </UserControl.Resources>
    <Grid Name="OrderGrid" Margin="0,10,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="0"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid.Resources>
            <CollectionViewSource x:Key="PurchaseReturnRecordList" Source="{Binding PurchaseReturnRecordList}">
                <CollectionViewSource.GroupDescriptions>
                    <PropertyGroupDescription PropertyName="StoOrderId"/>
                </CollectionViewSource.GroupDescriptions>
                <CollectionViewSource.SortDescriptions>
                    <componentmodel:SortDescription PropertyName="RecDate" Direction="Ascending"/>
                    <componentmodel:SortDescription PropertyName="StoOrderId" Direction="Ascending"/>
                    <componentmodel:SortDescription PropertyName="ProductId" Direction="Ascending"/>
                </CollectionViewSource.SortDescriptions>
            </CollectionViewSource>
            <local:CalculateTotalConverter x:Key="CalculateTotalConverter"/>
        </Grid.Resources>
        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <Label Content="{Binding ManufactoryName}" FontSize="32" Width="200" FontFamily="Segoe UI Semibold" Foreground="Black" Margin="10,5,0,0"/>
            <Rectangle Width="300"/>
            <Border Name="CloseAllIcon1" Tag="1" Height="40" Margin="5" MouseLeftButtonDown="Icon_OnMouseLeftButtonDown" MouseLeave="Icon_OnMouseLeave" MouseLeftButtonUp="Icon_OnMouseLeftButtonUp">
                <Image Source="../../Images/CloseRecord.png" Width="25" Margin="8,0,4,0" VerticalAlignment="Center"/>
            </Border>
            <Border Name="OpenOrderIcon" Tag="2" Height="40" Margin="5" MouseLeftButtonDown="Icon_OnMouseLeftButtonDown" MouseLeave="Icon_OnMouseLeave" MouseLeftButtonUp="Icon_OnMouseLeftButtonUp">
                <Image Source="../../Images/OpenRecord.png" Width="25" Margin="8,0,4,0" VerticalAlignment="Center"/>
            </Border>
            <Border Name="OpenAllIcon" Tag="3" Height="40" Margin="5" MouseLeftButtonDown="Icon_OnMouseLeftButtonDown" MouseLeave="Icon_OnMouseLeave" MouseLeftButtonUp="Icon_OnMouseLeftButtonUp">
                <Image Source="../../Images/OpenRecord2Detail.png" Width="24" Margin="8,0,4,0" VerticalAlignment="Center"/>
            </Border>
        </StackPanel>
        <DataGrid Grid.Row="1" Background="DarkSeaGreen" ItemsSource="{Binding Source={StaticResource PurchaseReturnRecordList}}" ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                  CellStyle="{StaticResource DataGridCellStyle}" Height="Auto" AutoGenerateColumns="False" BorderThickness="2" BorderBrush="DarkSeaGreen"
                  CanUserSortColumns="False" VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Disabled" PreviewMouseWheel="DataGrid_OnPreviewMouseWheel">
            <DataGrid.GroupStyle>
                <GroupStyle>
                    <GroupStyle.ContainerStyle>
                        <Style TargetType="{x:Type GroupItem}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type GroupItem}">
                                        <Expander IsExpanded="{Binding IsOpenAll, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, RelativeSource={RelativeSource AncestorType=local:ManReportControl}}" Background="White" Foreground="Black">
                                            <Expander.Header>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding Name}" Margin="5,0,0,0" Width="150" FontSize="22" FontFamily="Segoe UI Semibold"/>
                                                    <TextBlock Text="收貨日: " Margin="10,2,0,0" FontSize="16" FontFamily="Segoe UI Semi" VerticalAlignment="Center"/>
                                                    <TextBlock Text="{Binding Items[0].RecDate}" Margin="0,2,0,0" Width="90" FontSize="16" FontFamily="Segoe UI Semi" VerticalAlignment="Center"/>
                                                    <TextBlock Text="$" Margin="160,0,0,0" FontSize="20" FontFamily="Segoe UI Semibold"/>
                                                    <TextBlock Text="{Binding Converter={StaticResource CalculateTotalConverter}}" FontSize="20" FontFamily="Segoe UI Semibold"/>
                                                </StackPanel>
                                            </Expander.Header>
                                            <Expander.Content>
                                                <ItemsPresenter/>
                                            </Expander.Content>
                                        </Expander>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </GroupStyle.ContainerStyle>
                </GroupStyle>
            </DataGrid.GroupStyle>
            <DataGrid.Resources>
                <Style TargetType="{x:Type ScrollBar}">
                    <Setter Property="SnapsToDevicePixels" Value="True"/>
                    <Setter Property="OverridesDefaultStyle" Value="true"/>
                    <Setter Property="Width" Value="100"/>
                    <Style.Triggers>
                        <Trigger Property="Orientation" Value="Horizontal">
                            <Setter Property="Width" Value="Auto" />
                            <Setter Property="Height" Value="15" />
                            <Setter Property="Template" Value="{StaticResource HorizontalScrollBar}" />
                        </Trigger>
                        <Trigger Property="Orientation" Value="Vertical">
                            <Setter Property="Width" Value="15"/>
                            <Setter Property="Height" Value="Auto" />
                            <Setter Property="Template" Value="{StaticResource VerticalScrollBar}" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>
            <DataGrid.RowStyle>
                <Style TargetType="{x:Type DataGridRow}">
                    <Style.Triggers>
                        <Trigger Property="DataGridRow.IsSelected" Value="True">
                            <Setter Property="Background" Value="{StaticResource Shadow}"/>
                        </Trigger>
                    </Style.Triggers>
                    <Setter Property="Background" Value="{StaticResource Shadow}"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="FontSize" Value="16"/>
                    <Setter Property="Height" Value="41"/>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="商品代碼" Width="120" FontFamily="Segoe UI Semi" IsReadOnly="True" Binding="{Binding ProductId}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="HorizontalAlignment"  Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="商品名稱" Width="200" FontFamily="Segoe UI Semi" IsReadOnly="True" Binding="{Binding ProductName}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="HorizontalAlignment"  Value="Left"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="數量" Width="80" FontFamily="Segoe UI Semi" IsReadOnly="True" Binding="{Binding Amount}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="HorizontalAlignment"  Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="贈量" Width="80" FontFamily="Segoe UI Semi" IsReadOnly="True" Binding="{Binding FreeAmount}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="HorizontalAlignment"  Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="價格" Width="80" FontFamily="Segoe UI Semi" IsReadOnly="True" Binding="{Binding Price}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="HorizontalAlignment"  Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="小計" Width="90" FontFamily="Segoe UI Semi" IsReadOnly="True" Binding="{Binding SubTotal}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="HorizontalAlignment"  Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Orientation="Horizontal" Grid.Row="2" Background="{StaticResource GridDarkerBackGround}">
            <Rectangle Width="400"/>
            <Label Content="總金額: $" FontSize="20" FontFamily="Segoe UI Semibold" Foreground="Black" Margin="10,0,0,0" VerticalAlignment="Center"/>
            <Label Content="{Binding Total}" FontSize="20" Width="110" FontFamily="Segoe UI Semibold" Foreground="Black" VerticalAlignment="Center"/>
            <Border Name="CloseAllIcon2" Tag="1" Height="40" MouseLeftButtonDown="Icon_OnMouseLeftButtonDown" MouseLeave="Icon_OnMouseLeave" MouseLeftButtonUp="Icon_OnMouseLeftButtonUp">
                <Image Source="../../Images/CloseRecord.png" Width="25" Margin="8,0,4,0" VerticalAlignment="Center"/>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
